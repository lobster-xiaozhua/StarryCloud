| 发现时间 | 任务 | 严重度 | 描述 | 状态 |
|---------|------|--------|------|------|
| 2026-09-24 | T0 | 低 | 本机 npm 11.9.0 的 arborist 无法解析 `workspace:*` 协议（最小复现同样报错 EUNSUPPORTEDPROTOCOL）。已将 server/web 的 `@aiw/contracts` 依赖由 `workspace:*` 改为 `*`，npm 仍按工作区名称解析并软链本地包，行为等价。 | 已处理（偏离规格，已记录） |
| 2026-09-24 | T2 | 中 | 本环境 tsx 无法把 loader 传播进 `worker_threads`，导致 `.ts` worker 文件加载失败（"Unknown file extension .ts"）。已将 DB 进程由 `worker_threads` 改为 `child_process.fork` + `--import tsx` 启动子进程承载 better-sqlite3（仍不在主线程，符合规则 15 意图）。`RingBuffer.push` 改为 `push(seq, item)` 以便回填按序号过滤。 | 已处理（偏离规格，已记录） |
| 2026-09-24 | T6 | P1 | Dockerfile 漏 util-linux / procps（T8/T13 验证命令需要 setsid / pkill / ps） | fixed in T6-amend |
| 2026-09-24 | T6 | 中 | 沙箱容器网络无法访问 deb.debian.org / nodejs.org（DNS 被导向 captive 198.18.0.34），`docker build -f Dockerfile.sandbox` 在本环境无法完成，故 T6 验收命令未能在沙箱内实跑。Dockerfile 在正常联网机器上可正常 build。容器内测试（T7/T8/CP3）改为镜像缺失时显式打印 `[SKIP]` 后自跳过，并支持 `AIW_REQUIRE_DOCKER=1` 强制真跑（见 T8 行）。 | 已处理（环境限制，已记录） |
| 2026-09-24 | T7 | 低 | 规格 T7 启动参数写了 `--ulimit fsize=2g:2g`，但 Docker 27.5.1 的 `--ulimit` 解析器对 fsize 不接受 `g` 后缀（报错 `strconv.ParseInt: parsing "2g"`）。已改为等价字节值 `fsize=2147483648:2147483648`（2 GiB），软配额意图不变。 | 已处理（偏离规格，已记录） |
| 2026-09-24 | T8 | 中 | 规格 T8 命令封装为 `setsid timeout -k 5 <ts> bash -c cmd`。实测在 Docker 27.5.1 下 `setsid` 会让 `docker exec` 的退出码恒为 0（setsid 父进程先退出，docker 只拿到父进程退出码），导致 (a) `exit 3` 这类退出码丢失、(b) 超时无法靠退出码 124 识别。已改为：`docker exec ... bash -c cmd`，超时与 abort 改在 Node 侧用定时器/AbortSignal 实现，触发时通过 `pkill -P 1`（规格兜底的杀进程组方式）清理容器内命令。 | 已处理（偏离规格，已记录） |
| 2026-09-24 | T8 | P1 | 原 `OutputBuffer` 用 `full += chunk` 累积整段输出、且每块做一次 `Buffer.byteLength(full)`。在 100MB 级输出下字符串拼接退化为 O(N²)（V8 rope 节点约 13 万），实测单次 exec 耗时 **37.3s**（raw `docker exec` 仅 0.09s），会误触发 30s 测试超时。已改为「头部缓存 + 滚动尾部缓冲 + 全量边收边落临时文件」，常驻内存 ≤ ~24KB；复测同一命令 **353ms**（提速约 105×），`truncated/stdoutBytes/note` 结果不变。 | 已处理（缺陷修复，已记录） |
| 2026-09-24 | T8 | P2 | 容器测试原为「镜像缺失静默跳过」，无法区分「跳过」与「通过」。已改为：跳过时打印 `[SKIP] aiw-sandbox:latest not found. Run: docker build -f Dockerfile.sandbox -t aiw-sandbox:latest .`；并新增 `AIW_REQUIRE_DOCKER=1` 环境变量，置 1 时镜像缺失即令套件失败，供 CI / 本地强制真跑。 | 已处理（流程补强，已记录） |
| 2026-09-24 | T12 | P0 | 前端 SSE 解析与契约不符：`app.tsx` 按 `{data:{...}}` 包裹体读取（`ev.data.content` / `ev.data.toolCallId`），但服务端 `runs.ts` 写入的是**负载本身**（`data: ${JSON.stringify(ev.data)}`，即 `{"seq":1,"content":"..."}`）。契约 `@aiw/contracts/events.ts` 的 `SSEEvent.data` 才是那一层。结果：**首个 delta 即抛 `TypeError: Cannot read properties of undefined (reading 'content')`，助手正文永远为空、工具块永不渲染** —— 整条聊天链路实际不可用。此前未被发现，是因为 T4 集成测试只断言 `data: ` 前缀存在、前端只做了构建检查，没有任何测试真正解析过 SSE 负载。已改为直接读负载层（delta/tool_start/tool_delta/done/error），并按契约把 `tool_end.data.output` 当作 `ToolOutput` 对象解包（原来按字符串处理）。已用 Chromium 真实驱动前端跑通：badge `exit 0`、正文与工具块均正确（见 T12 报告）。 | 已处理（缺陷修复，已记录） | ① 规格关键点 2 的「delta 落库每 500ms 节流 flush」依赖消息内容更新接口，但 DB 层（T2 冻结 API）只有 append/updateStatus，无 update-content，故改为流结束后一次性落库 assistant 消息（SSE delta 仍逐字实时推送，落库顺序/seq 连续性验收均满足）。② `runAgent` 增加可选 `opts:{provider?, toolRegistry?}` 以支持规格要求的 mock provider + mock 工具测试（默认仍用真实 openaiProvider/tools）。 | 已处理（偏离规格，已记录） |
| 2026-09-24 | T10 | 低 | ① 规格关键点 2 的「delta 落库每 500ms 节流 flush」依赖消息内容更新接口，但 DB 层（T2 冻结 API）只有 append/updateStatus，无 update-content，故改为流结束后一次性落库 assistant 消息（SSE delta 仍逐字实时推送，落库顺序/seq 连续性验收均满足）。② `runAgent` 增加可选 `opts:{provider?, toolRegistry?}` 以支持规格要求的 mock provider + mock 工具测试（默认仍用真实 openaiProvider/tools）。 | 已处理（偏离规格，已记录） |
