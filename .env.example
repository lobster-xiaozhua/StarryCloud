# 复制为 .env 后填入真实值（.env 已被 .gitignore 忽略）
# 任意 OpenAI 兼容服务均可

OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxx
OPENAI_BASE_URL=https://token.sensenova.cn/v1
MODEL=sensenova-6.8-flash-lite

# 后端监听端口（默认 3000）
PORT=3000

# 数据目录（SQLite 与各会话 workspace，默认 ./data）
# DATA_DIR=./data
